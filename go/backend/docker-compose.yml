version: "3.8"
services:
  dev-mysql:
    image: mysql:latest
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: momokuro
      MYSQL_USER: momokuro
      MYSQL_PASSWORD: momokuro
      MYSQL_DATABASE: momokuro
    restart: always
    networks:
      - lesson
networks:
  lesson:

